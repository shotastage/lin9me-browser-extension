doctype html
html
    head
        meta(charset="utf-8")
        link(rel="stylesheet" href="popup.css")

    body(style="margin: 0 !important; border-radius:10px;")
        .container
            .render
                .ball-scale-ripple-multiple#loading
                    div
                    div
                    div

                h2.url-area.invisible#shortened_url
                h3.invisible#access_count
            p#message クリップボードにコピーしました

        script(src="../popup.js")
